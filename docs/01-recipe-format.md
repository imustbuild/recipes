# Recipe Format

## Location
```
content/recipes/<slug>/index.md
```

## Rules
- Missing keys are allowed
- Missing = undefined, never null
- Only identity fields required
- YAML frontmatter + optional Markdown body

## Time Fields
```
times:
  prep_minutes: 15
  cook_minutes: 55
  total_minutes: 70
```
